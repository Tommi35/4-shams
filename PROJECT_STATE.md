# PROJECT_STATE — 4 Shams virtual CD player

> Single source of truth. Read this first at the start of every session. If anything here contradicts the files on disk, STOP and reconcile before changing code.

## Current phase / task
LIVE at https://tommi35.github.io/4-shams/ (verified, refinement deployed). Refinement per REFINEMENT_PLAN.md — **Blocks A (catalog), B (boombox restyle), C (stickers), D (final local verification) DONE + verified**; **E1–E3 (deploy via temp clone + live verify) DONE**; only E4 (real-phone Shams-test v2) remains. Then **VISUAL_REVAMP_PLAN.md** (Sony CFD-330 realism pass) in progress: **BLOCK 1 (silhouette), BLOCK 2 (CD rainbow disc + printed label), BLOCK 3 (buttons/controls material) DONE + verified headlessly**; Block 4 explicitly **skipped for this pass**; next = BLOCK 5 (flanking round speakers), then (6), 7.
> **NEW (2026-09-19):** `VISUAL_REVAMP_PLAN.md` delivered — planning-only realism pass toward the real Sony CFD-330 (wide boombox silhouette, rainbow CD sheen + printed label, matte pill controls, flanking round speakers, metal handle + antenna, repositioned stickers, optional decorative cassette). **BLOCK 1 (silhouette & proportions) DONE + verified headlessly** (3-zone flanked grid, squat landscape aspect 1.07–1.22 confirmed at 320/375/390/768/1280, LCD/controls/volume/stickers/lid all inside, tap targets ≥44px with document 320 exception re-compacted to 40/44px, zero console errors, screenshots `shot-4-*.png`). **BLOCK 2 (CD disc rainbow realism) DONE + verified headlessly** (full-spectrum desaturated conic bands + a second low-opacity rainbow conic at a different start angle so bands moiré while spinning, groove texture kept, spindle ring retuned, `.disc-label` redesigned as a printed cream sticker — wordmark "4 SHAMS" + "14 TRACKS · 51 MIN", paper grain, die-cut shadow, charcoal ink, `aria-hidden` + `pointer-events:none`; reverse spin moved onto the label, `prefers-reduced-motion` → 9s; behavior invariant, `js/app.js` untouched; state-machine smoke, reduced-motion, 5-viewport layout audit, zero console/page/network errors + css/js/mp3 all 200 all PASS; screenshots `shot-2-b2-{off,open,ready,playing,desktop}.png`). **BLOCK 3 (buttons/controls material) DONE + verified headlessly** (glossy cream circles → matte charcoal pill keys — ~30–34px tall, fine ~10% top bevel, no sheen — on ≥44px tap boxes; `.ctrl-play` keeps no brass, distinguished only by size + solid white glyph; transport icons redrawn as printed-white svg glyphs, `aria-label` + disabled logic untouched; volume → black/white slider with matte charcoal notched thumb + grip ridge; all brass/gold swept off device surfaces onto the booklet/tracklist only; `.bg` tray recess + well now neutral charcoal; state-machine invariant, `js/app.js` untouched; 7 material checks + state-machine smoke + 5-viewport layout audit 320/375/390/768/1280 + zero console/page/network errors + css/js/mp3 all 200 — ALL PASS; screenshots `shot-2-b3-{off,open,ready,playing,desktop}.png`). **Block 4 SKIPPED for this pass (per user); remaining: BLOCK 5 → (6) → 7.**

## What's done (VERIFIED by me this session)
- Project folder `C:\Users\xale\Desktop\4 Shams` — self-contained.
- Audio: **14** mp3 tracks at 160 kbps into `/audio` (`01-…` … `14-…`), per the locked 14-track order; id3 title/artist embedded. Source files untouched in `C:\Users\xale\Desktop\disco Shams`.
  - Removed: `10-here-comes-the-sun`, `12-weak-for-your-love`, `14-masterpiece`. Renamed: `11-is-this-love`→`10`, `13-window`→`11`, `15-amore…`→`12`, `16-kiss-of-life`→`13`, `17-apocalypse`→`14`.
- `index.html` — top-loading portable CD player: flip-up lid over a disc, LCD, transport buttons (prev/play/next/eject), volume + mute, track booklet (says `14 TRACKS · 51 MIN`), footer.
- `css/styles.css` — plain CSS; mobile-first; warm cream/brass + charcoal boombox palette; pastel floral sticker decor (Block C); no external fonts/services.
- `js/app.js` — plain JS: power state machine (off → open → closing → reading → ready → playing/paused), audio engine, direct track select, volume/mute, LCD marquee (measured Web Animations, reduced-motion aware), graceful error/stall hints. `TRACKS` length = 14.
- `robots.txt` (Disallow /) + `<meta name="robots" content="noindex,nofollow">`.

## Testing I actually ran (real headless Chrome, DevTools Protocol)
- 23-check end-to-end suite (pre-refinement), plus Block A suite (14 rows, direct-select all 14, audio 200/206) — all PASS.
- **Block B 49-check suite (post-restyle):** branding split checks (SONY CFD-330 nameplate, lid/lcd SONY, disc/booklet/footer/title 4 SHAMS, STEREO grille mark), full state-machine smoke (off→open→closing→ready→playing, next/prev/pause-freeze/direct-select/volume/mute/eject-all-PASS), layout audit 320/375/390/768/1280 — no horizontal scroll, device fits viewport, tap targets ≥54px, audio all 200/206, **zero console errors / uncaught exceptions**.
- Marquee animation running in ready & playing; progress bar updates (0:02 / 3:48, 1%).
- **Block C 42-check suite (post-stickers):** 6 inline-SVG stickers present (all `aria-hidden`, `pointer-events:none`, `user-select:none`); no sticker rect intersects LCD / prev / play / next / eject / volume slider / mute; lid sticker fully inside the lid; all stickers inside the device; zero horizontal scroll + device fits viewport + tap targets ≥54px at 320/375/390/768/1280; full state-machine regression (off→open→closing→ready→playing, next/prev/pause-freeze/direct-select/volume/mute/eject) unchanged; audio all 200/206; zero console errors / uncaught exceptions.
- **Block D 62-check suite (final combined A+B+C):** 14 rows + conclusive CSS counter-chain check (reset/inc/content decimal-leading-zero + 14 `.t-num`), booklet `14 TRACKS · 51 MIN`, direct-select all 14 → correct src + plays, removed tracks never requested, all boombox branding/anatomy splits, 6 stickers non-interactive, full state-machine smoke (off→open→closing→ready→playing→paused, next/prev/direct-select/vol/mute/eject), sticker-rect audit + no h-scroll + device fits + tap targets ≥54 at 320/375/390/768/1280, audio all 200/206, **`file://` double-click check PASS (renders off + 14 rows + no script errors)**, zero console errors. Screenshots `shot-2-{off,open,ready,playing,desktop-ready,file}.png`.
- **Block E1–E3 (deploy + live verify):** temp clone synced with `git rm` (×3) + `git mv` (×5, renames at 100% similarity — history preserved), app/css/html/docs copied, committed `c34ee5a`, pushed `main` (4335140..c34ee5a). Live: index/css/js 200, renamed audio (`10-is-this-love`, `14-apocalypse`) 200, **removed tracks all 404**. Headless render of the LIVE URL — 11/11 PASS: 14 rows, 6 stickers, SONY CFD-330, ready→playing over live with audio 200/206, zero console errors. Screenshot `shot-live.png`. Deploy is fully live.
- **VISUAL_REVAMP blocks (headless CDP):** Block 1 → 10-check layout audit + 18-check smoke; Block 2 → 9-check disc anatomy + 14-check smoke + reduced-motion + 10-check layout ×5 viewports; **Block 3 → 7-check control-material + state-machine smoke (off/open → prev/play/next/vol/mute disabled while eject stays enabled; ready → all enabled; scrub/mute/pause-freeze unchanged) + 5-viewport layout audit (320/375/390/768/1280 — no h-scroll, tap ≥44px, pill 30–34px, no overlaps) + zero console/page/network errors + css/js/mp3 all 200 — ALL PASS**, `js/app.js` untouched throughout.
- Screenshots saved for the user (this model cannot view images):
  `C:\Users\xale\AppData\Local\Temp\opencode\shot-mobile-{off,open,ready,playing}.png`, `shot-desktop-ready.png` (pre-refine), `shot-2-ready-a.png` (Block A), `shot-2-{ready,playing,desktop-ready}.png` (Block B), and Block C stickers: `shot-3-{ready,playing,desktop}.png`.

## What is NOT yet verified — needs the USER
- Aesthetics / real-phone feel + real audio playback on a phone browser & mobile data (the Shams-test).

## Known bugs / limitations
- Audio errors show a hint and return to ready (last-resort; not provoked in tests).
- Netlify Drop 100 MB limit: irrelevant now — GitHub Pages in use (no such limit).
- GitHub web-UI upload refused a ~49 MB audio batch with "file too large" (no single file >25 MB); solved by `git clone` + `git push` from this machine instead.
- Single shared <audio> element: no crossfade (by design, keeps it simple).

## Decisions made
- English interface. Names: folder/site/disc = `4 Shams`. No personal note. The `.webp` in Downloads is unrelated (ignored).
- Typed 17-track list is the authoritative order (numbers on 3 source filenames are stale) → **refined to the locked 14-track order in REFINEMENT_PLAN.md** (3 removed, renumbered 01–14).
- Re-encoded to 160 kbps mp3. Originals untouched.
- Design: portable top-loading CD player, flip-up lid, warm cream/brass, LCD marquee, spin-on-play, autoplay-safe flow.
- **Refinement (locked in REFINEMENT_PLAN.md):** player becomes a Sony CFD-330 boombox (brand on the player); disc/album/booklet/footer/site stay `4 SHAMS`; pastel floral stickers; playback logic & state machine unchanged.
- **Blocks A–E status:** A (catalog) ✓, B (boombox restyle) ✓, C (stickers) ✓, D (final local verification) ✓, E1–E3 (deploy + live verify) ✓ — all headless-verified. **E4 (real-phone Shams-test v2 on the live URL) is the user's handoff step.** Desktop files and live site are in sync; temp clone is current (commit `c34ee5a`).
- Single <audio> element, no frameworks, no build step, no external requests.

## Needs my input (open decisions)
1. **Access control:** DECIDED — keep the site **unlisted** (robots.txt + noindex; open to anyone with the link; not indexed, not private). No password gate.
2. **Deploy target:** DECIDED — **GitHub Pages** (browser-based upload, no git skills needed, no size concerns for ~98 MB). Deploy step needs the user's free GitHub account → guided one step at a time.

## Files in the project (current)
- `index.html` — page + player + booklet markup
- `css/styles.css` — all styling
- `js/app.js` — all logic + track list (14)
- `audio/01-walking-into-sunshine.mp3` … `audio/14-apocalypse.mp3`
- `robots.txt` — no-index
- `PROJECT_STATE.md`, `TODO.md`, `REFINEMENT_PLAN.md` — state files

## Preview / deploy
- Preview: double-click `index.html` in the folder (verified working over `file://`). For best fidelity serve it: `python -m http.server` inside the folder, open `http://127.0.0.1:8000/`.
- **LIVE: https://tommi35.github.io/4-shams/** — GitHub Pages, unlisted (robots.txt + noindex), open to anyone with the link.
- How it got there (for reference): user created GitHub account (Tommi35); web-UI upload failed on audio batch → used git already installed on this machine: `git clone` repo in Temp, copied files, `git add -A && git commit && git push` (one-time browser sign-in), then Settings → Pages → Deploy from branch `main` / root.
- Future updates: same temp clone at `C:\Users\xale\AppData\Local\Temp\opencode\deploy-4shams` (copy new files → commit → push), or simply re-do via GitHub web UI for small changes.

## Progress log
- 2026-09-14 — Read prompt; asked section-3 questions; got names/lang/order; found track-order conflict; user confirmed typed order.
- 2026-09-14 — Encoded 17 tracks to /audio (160 kbps); scaffold + TODO + PROJECT_STATE created.
- 2026-09-14 — Block 1 (structure/CSS) built; served locally OK.
- 2026-09-14 — Block 2/3/4/5 built (JS audio engine, spin, opening sequence, marquee); 23-check headless-Chrome suite PASS; marquee/progress verified; layout audit clean; robots.txt added.
- 2026-09-14 — `file://` (double-click) verified. Decisions: unlisted + GitHub Pages. Ready for user hand-off.
- 2026-09-14 — DEPLOYED: user created account (Tommi35) + repo `4-shams`; web upload failed on audio ("file too large", irrelevant to real sizes) → pushed via git; Pages enabled on `main`; live at https://tommi35.github.io/4-shams/, HTTP 200, app.js + audio verified. Next: real-phone Shams-test.
- 2026-09-14 — **Block A (catalog cleanup) DONE + verified**: TRACKS → 14 rows; removed `10-here-comes-the-sun`, `12-weak-for-your-love`, `14-masterpiece`; renamed 5 → new numbers; booklet `14 TRACKS · 51 MIN`. Headless-Chrome suite: 14 rows render, counter chain 01–14, direct-select all 14 = correct src + plays, all audio 200/206, removed tracks never requested, zero console errors. Screenshot `shot-2-ready-a.png` saved.
- 2026-09-14 — **Block B (boombox restyle) DONE + verified**: SONY CFD-330 nameplate + handle recess, smoked translucent lid (printed SONY) keeping flip timing, deck plate around LCD+keypad, chamfered keycaps + engraved glyphs, VOL label/ticks/knurled thumb, grille with STEREO, rubber feet, charcoal body with grain/bevel/seam. Off-state grayscale kept. 49-check headless suite — all PASS, layout clean at 320–1280, zero console errors. Screenshots `shot-2-{ready,playing,desktop-ready}.png`. Block C next.
- 2026-09-14 — **Block C (pastel floral stickers) DONE + verified**: added 6 self-contained inline-SVG stickers — daisies ×2, clover, tiny sun, heart, tiny star in pink/mint/peach/sky/cream — all `aria-hidden`, `pointer-events:none`, `user-select:none`. Cluster on the deck plate's blank right column, heart wrapping the grille's top edge (right cap), star on the lid's left corner (rotates with flip; lid stays clickable). 1–3° tilt + worn drop-shadow + matte paper gradient backing. 42-check headless suite all PASS: no sticker overlaps LCD/buttons/volume slider/mute, all stickers inside device & lid star inside lid, no horizontal scroll + device fits + tap targets ≥54px at 320/375/390/768/1280, full state-machine regression unchanged (off→open→closing→ready→playing, next/prev/pause/direct-select/vol/mute/eject), audio 200/206, zero console errors. Screenshots `shot-3-{ready,playing,desktop}.png`. Block D (final combined verification) next.
- 2026-09-14 — **Block D (final combined verification) DONE + verified**: one merged 62-check suite re-runs A+B+C together — all PASS. Catalog (14 rows, conclusive CSS counter-chain, `14 TRACKS · 51 MIN` booklet, direct-select all 14 correct src + plays, removed tracks never requested), boombox identity + anatomy, 6 stickers non-interactive, full state-machine smoke (off→open→closing→ready→playing→paused, next/prev/pause-freeze/direct-select/vol/mute/eject), sticker-rect audit + no h-scroll + device fits + tap targets ≥54 at 320/375/390/768/1280, audio all 200/206. `file://` double-click check PASS (renders, 14 rows, no script errors). Screenshots `shot-2-{off,open,ready,playing,desktop-ready,file}.png`. Ready to deploy — Block E next (needs go-ahead).
- 2026-09-14 — **Block E1–E3 (deploy + live verify) DONE**: temp clone synced (git rm ×3, git mv ×5 at 100% — history preserved), app/css/html/robots/docs copied, `REFINEMENT_PLAN.md` added to repo, committed `c34ee5a`, pushed `main` (4335140..c34ee5a). Live HTTP: index/css/js 200, renamed audio 200, removed tracks all 404. Headless render of live URL 11/11 PASS (14 rows, 6 stickers, SONY CFD-330, ready→playing, audio 200/206, zero console errors); `shot-live.png` saved. **E4 = real-phone Shams-test v2 — user handoff.**
- 2026-09-19 — **VISUAL_REVAMP_PLAN.md written (plan only)**: analysed current device markup/CSS vs. the real Sony CFD-330 description; defined Blocks 1–7 with TODO-markers, dependencies and open decisions (photo bytes not delivered; tap-target threshold relax to ≥44px). No code changed. Awaiting go-ahead to execute BLOCK 1. E4 (phone test) still open; will recheck after the revamp blocks.
- 2026-09-19 — **BLOCK 1 (silhouette & proportions) DONE + verified**: 3-zone boombox grid [round spk | center command stack (tray+deck+vol) | round spk], landscape body (1.07–1.22:1 at 375/390/768/1280, 0.91:1 squat at 320), disc/tray/LCD/keys/slider scaled down, `<640px` narrow fallback shrinks speakers first, all absolute-px children re-pinned; `.grille` hidden (Block 4 will remove it); state machine + all `[data-state]` hooks untouched. Headless: 10-check layout audit + 18-check smoke at 320/375/390/768/1280 — no h-scroll, device fits, controls ≥44px (documented exception), zero console errors. Screenshots `shot-4-*.png`.
- 2026-09-19 — **BLOCK 2 (CD disc rainbow realism) DONE + verified**: `.disc` rebuilt with layered full-spectrum desaturated conic bands (violet→red) over the kept groove texture + second low-opacity rainbow conic (40° vs 168°) so bands sweep/moiré during rotation; spindle ring (`.disc::before`) retuned as clear-plastic ring; `.disc-label` redesigned as printed cream sticker (wordmark 4 SHAMS + 14 TRACKS · 51 MIN, concentric tint + paper grain, die-cut shadow, charcoal ink, aria-hidden + pointer-events:none); reverse spin moved onto the label with a dedicated `spin-label` keyframe; `prefers-reduced-motion` → 9s for both layers. `index.html` (label markup) + `css/styles.css` only — `js/app.js` untouched. Headless suite: 9-check disc anatomy + 14-check state-machine smoke (boot off→lid open→disc load ready→play→playing, spin running, label reverse running, pause freeze, next/prev/mute/eject/direct-select unchanged, reload-after-eject) + reduced-motion 9s on disc & label + 10-check layout audit ×5 viewports (no h-scroll, disc in tray, label in disc, stickers in device & clear of controls, controls ≥44px, lid covers tray) + zero console/page/network errors with all css/js/mp3 200 — **ALL PASS**. Screenshots `shot-2-b2-{off,open,ready,playing,desktop}.png`. **Block 4 SKIPPED per user; Block 3 next.**
- 2026-09-20 — **BLOCK 3 (buttons/controls material) DONE + verified**: glossy cream circles → matte charcoal pill keys (~30–34px tall, fine ~10% top bevel, no sheen) behind ≥44px tap boxes; `.ctrl-play` brass removed — distinguished by size + solid printed-white glyph only (48×46, `--glyph-solid`); transport icons redrawn as printed-white svg glyphs (22px `--glyph`, opacity .88, drop-shadow); `aria-label` + `:disabled` untouched (dim 0.4); volume slider → black/white with `--track-on/--track-off` fill via `--vol`, matte charcoal notched thumb (16px) + vertical grip ridge, mute matches, disabled dimmed; open recess/tray glow/well → neutral charcoal; `--brass*` now booklet-only. Headless suite: 7 material checks + state-machine smoke (off/open → 5 controls disabled, eject always enabled; ready → all enabled; scrub/mute/pause-freeze unchanged) + 5-viewport layout audit (320/375/390/768/1280, no h-scroll, ≥44px, pills 30–34px) + zero console/page/network errors + css/js/mp3 all 200 — **ALL PASS**, `js/app.js` not modified. Screenshots `shot-2-b3-{off,open,ready,playing,desktop}.png`. Deployed (pushed with Block 2). **Next: BLOCK 5 (flanking round speakers), then (6), 7.**